[build]
  functions = "netlify/functions"
  publish = "."

[build.environment]
  NODE_VERSION = "18"

[[redirects]]
  from = "/impact-logic/submit"
  to = "/.netlify/functions/submit"
  status = 200
  force = true

[functions]
  node_bundler = "esbuild"

[functions."submit"]
  timeout = 60
